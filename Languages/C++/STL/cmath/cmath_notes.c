#include math

int main() {
    //sqrt() returns the square root of a number

    //abs() returns the absolute value of a number. 
    // Need to use std:: to specify which abs() function you are actually using 
    // since there is more than one library with this function.

    return 0;
}