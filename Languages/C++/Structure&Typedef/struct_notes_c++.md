# What is a 'struct'?
A programmer-defined data type that lets multiple variables to be grouped together.
- Multipl variables/fields of the same type can be declared in a comma-separated list: 'string name, address;'.
- variables/fields are public by default.