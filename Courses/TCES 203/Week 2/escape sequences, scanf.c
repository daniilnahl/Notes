#include <stdio.h>
/****
 * PARTIAL SEQUENCE
 * alert(bell) -> \\a
 * backspace -> \\b
 * new line -> \\n
 * horinzontal tab \\t
 */

 /***
  * SCANF FUNCTION
  * scanf - reads input according to a particular format. Its 
  * format string may contain both ordinary chars and conversions specs.
  */
int main(){
//scanf example
    int i, j;
    float x, y;
    scanf("%d%d%f%f", &i, &j, &x, &y);

    return 0;
}